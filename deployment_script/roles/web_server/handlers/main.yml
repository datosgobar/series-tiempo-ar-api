---

- name: Reload systemd
  command: systemctl daemon-reload

- name: Enable and restart web server
  service:
    state: restarted
    name: "{{ item }}"
    enabled: yes
  tags: quickly
  with_items:
    - gunicorn.socket
    - nginx.service
